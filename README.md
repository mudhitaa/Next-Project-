# no need routing 
- routing is done by folder and file 
- uses `app/` folder

- naming convention 
-css is preloaded with tailwind


## next applicaationn
- entry point -> layout.tsx
    - page.tsx content as child of layout.tsx

## file convention
- app/<folder> is used for routin if the folder contains the following files
-layout , template,error,loading,not-found,page  -> every file must have default export
- main use layout and page 
-folder name and routeing paath should be exatctlly the same